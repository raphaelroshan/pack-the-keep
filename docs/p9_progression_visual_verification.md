# P9 Progression Visual Verification

The equipped Roadside Intelligence state was captured from a real Godot display at 1280×720. The Preparation screen showed `CAMPAIGN LEDGER — EQUIPPED`, the modifier name, its one-morale trade-off, limitation, and a visible `Unequip for next run` action near the top of the command table.

The main forecast simultaneously showed `Actors: Raider, Raider` and stated that targets still respond to keep condition. This keeps the unlock's information benefit distinct from Scout target revelation and avoids implying that the modifier predicts the correct response.

The ledger, fort, status line, and forecast fit the target frame without horizontal clipping. The temporary capture image and harness were removed after inspection; headless UI coverage verifies the LOCKED, UNLOCKED, and EQUIPPED states.
