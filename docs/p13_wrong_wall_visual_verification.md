# P13 Wrong Wall Chain Visual Verification

## Capture

- Build: `0.13.2-wrong-wall-chain`
- Viewport: 1280×720 windowed
- Screen: Results after an early Wrong Wall collapse
- State: forecast declined, Workshop recovery deferred under scarcity, terminal report active

## Observed

- `AUTHORED EVENT — The Wrong Wall | RESULTS` is visible at the top of the command table.
- The report setup and resource-free conclusion action fit without horizontal clipping.
- The Workshop remains visibly strained on the ground-floor board, preserving the spatial consequence beside the report.
- Campaign Ledger history below the active event shows the two earlier chain choices newest-first.
- The command-table scrollbar remains visible and the event panel does not cover the fort.

The capture was used only for local inspection and removed afterward. Headless coverage verifies the warning decline, scarcity fallback, authoritative repair path, active-event save/load, normal completion, early-collapse conclusion, deterministic replay, and generic UI flow.
