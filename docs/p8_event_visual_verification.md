# P8 Authored Event Visual Verification

The Relief Road forecast event was captured from a real Godot display at 1280×720. The Preparation screen showed `AUTHORED EVENT — The Bell Has a Pattern | PREPARATION` near the top of the scrollable command table, followed by the full setup, consequence text, and both legal choices.

The shortened button labels fit the command column without horizontal clipping. The title wraps cleanly, the scrollbar remains visible, and the event card does not obscure the two-floor keep. The main guidance and quick-test status both identify the unresolved event gate before asking the player to start battle.

The capture was used only for local inspection and was removed afterward. Headless UI coverage separately verifies the forecast, recovery, and conclusion panels and the final `EVENT CONSEQUENCES` report.
