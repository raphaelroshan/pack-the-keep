# P68 — Phase-Specific Framing Verification

Build `0.55.0-phase-framing` gives every major screen one truthful decision frame. Preparation names the keep's opening and forecast doctrine. Assault distinguishes tick-zero readiness, paused inspection, and live resolution. Recovery states its remaining action budget and next pressure. Terminal Results keeps the causal-review and replay framing.

`PhaseHeaderSnapshot` is a deterministic read-only projection. Repeated builds are checked against unchanged serialized keep state in `tests/test_k3_screen_presentation_snapshots.gd`; the complete tutorial, responsive, recovery, all-phase persistence, and campaign suites remain enforced by `scripts/verify.sh`.

Normal-renderer evidence is stored under `docs/visual_evidence/v0.55.0-phase-framing-review-2026-09-02/`. Human comprehension and owner distribution approval remain pending.
